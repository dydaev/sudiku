import type IField from "../intrfaces/IField";

type numField = number;
type numSquare = number;
type lineOrdinate = "x"|"y";
type board = [][IField];

export type {
	numField,
	numSquare,
	lineOrdinate,
	board
}
